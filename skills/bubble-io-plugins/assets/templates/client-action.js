// Client-side action: runs in the browser.
// Receives: instance (for element actions), properties, context.
// For standalone workflow actions (not element-bound): no instance argument.

// Bubble.io code starts below. Please ignore code above when adding to Bubble.io
let action = function(instance, properties, context) {

  // -- Load all data first (before side effects) --
  // var value = properties.my_field;

  // -- Perform the action --
  // Example: trigger analytics, manipulate DOM, tokenize a card, etc.

  // -- (Optional) Update exposed states --
  // instance.publishState('action_result', value);
  // instance.triggerEvent('action_completed');

};
