// Bubble.io code starts below. Please ignore code above when adding to Bubble.io
let action = function(instance, properties, context) {
  /**
   * @description Client-side workflow action for PLUGIN_PREFIX.
   *
   * @param {object} instance   - Bubble element instance (publishState, triggerEvent, canvas, data).
   *                              Not available for standalone (non-element) actions.
   * @param {object} properties - Action parameter values from the Bubble editor.
   * @param {object} context    - Bubble context (keys, currentUser, etc.)
   */

  // if (properties.verbose_logging) {
  //   console.log('[PLUGIN_PREFIX] action triggered', { properties });
  // }

  // var value = properties.my_field;

  // instance.publishState('action_result', value);
  // instance.triggerEvent('action_completed');

};
